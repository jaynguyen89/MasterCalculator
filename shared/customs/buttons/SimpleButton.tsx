import React from 'react';
import { Button, Icon } from 'react-native-elements';

import { ISimpleButton } from '../../models';
import styles from './styles';

const SimpleButton = (props : ISimpleButton) => {
    return (
        <Button>
        
        </Button>
    );
}

export default SimpleButton;
